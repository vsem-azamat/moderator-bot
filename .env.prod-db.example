# Production Database Connection
# Copy this file to .env.prod-db and fill with your production DB credentials

# Production Database Configuration
DB_HOST=your-production-db-host.com
DB_PORT=5432
DB_USER=your_prod_user
DB_PASSWORD=your_prod_password
DB_NAME=moderator_bot_prod

# Override environment to indicate prod DB usage
ENVIRONMENT=development-with-prod-db

# Optional: Override logging for prod DB operations
LOG_LEVEL=DEBUG
